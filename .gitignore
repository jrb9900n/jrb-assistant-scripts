node_modules/
logs/
*.log
scheduler/logs/
teams/logs/
core/logs/
*.backup-*
sa-session-cache.json
sa-incapsula-backoff.json
qbo-sync-watchdog-state.json

# Runtime state (not source)
data/
self-heal-queue.json

# Ad hoc diagnostic/investigation scripts and scratch working dirs — never
# meant to be committed; see .archive/ on the live machine for past batches
probe-*.mjs
sa-js-cache-*
scratch/
